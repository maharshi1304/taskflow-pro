stages:
  - verify

runner_verify:
  stage: verify

  image: docker:27-cli

  tags:
    - docker
    - taskflow
    - aws

  script:
    - echo "Checking Docker access..."
    - unset DOCKER_HOST
    - docker version
    - docker info
    - docker ps